#include <bits/stdc++.h>
using namespace std;
int d;
string k; 
vector<vector<vector<int>>> dp;  
vector<int> numK; 
int main(){
    if (fopen("test.inp", "r")){
        freopen("test.inp", "r", stdin);
        freopen("test.out", "w", stdout); 
    }
    cin >> k >> d;
    numK.r
    return 0;
}